name: app_clima
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  flutter_riverpod: ^2.5.1
  go_router: ^17.0.0
  dio: ^5.7.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  intl: 0.20.2
  geolocator: ^14.0.2
  # Firebase (sin Functions)
  firebase_core: ^4.4.0
  firebase_auth: ^6.1.4
  cloud_firestore: ^6.1.2
  google_sign_in: ^7.2.0
  flutter_gen: ^5.12.0

  flutter_map: ^8.2.2
  latlong2: ^0.9.1
  image_picker: ^1.2.1
  firebase_storage: ^13.0.6
  file_picker: ^10.3.10

dev_dependencies:
  build_runner: ^2.4.11
  freezed: ^3.2.3
  freezed_annotation: ^3.1.0
  json_serializable: ^6.9.0
  json_annotation: ^4.9.0


flutter:
  uses-material-design: true
  generate: true
  assets:
    - assets/data/peru_cities.json
